# Security Policy

- Allowed roots: `ALLOWED_ROOTS` from `.env`.
- Creation of new files is allowed.
- Destructive actions require explicit approval via Telegram.
- System settings and shell configuration changes require manual approval.
- Approvals are handled via Telegram, not local interactive prompts.
